import 'package:flutter/material.dart';

class HomeDaily extends StatelessWidget {
  const HomeDaily({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("HomeDaily"),
    );
  }
}
