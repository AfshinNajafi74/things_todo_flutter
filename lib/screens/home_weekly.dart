import 'package:flutter/material.dart';

class HomeWeekly extends StatelessWidget {
  const HomeWeekly({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Center(
      child: Text("HomeWeekly"),
    );
  }
}
